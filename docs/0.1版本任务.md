# 0.1 版本任务

## 目标：基于 Zig + SDL3 + Flecs 实现 Windows 平台贪吃蛇游戏，验证引擎核心最小闭环

---

### 1. 项目环境搭建与基础配置

- [ ] 创建 Zig 项目基础结构（`build.zig`、`src` 目录）

- [ ] 集成 SDL3 库（配置 `build.zig` 链接 SDL3、包含头文件）

- [ ] 集成 Flecs 库（配置 `build.zig` 链接 Flecs、包含头文件）

- [ ] 编写最小测试程序（初始化 SDL3 + Flecs，创建窗口并运行空主循环）

---

### 2. 引擎核心基础模块（窗口与输入）

- [ ] 封装 SDL3 窗口创建与管理（`Window` 结构体：初始化、销毁）

- [ ] 封装 SDL3 事件循环处理（`EventLoop`：处理窗口关闭、输入事件）

- [ ] 封装基础键盘输入状态管理（`InputState`：记录按键按下/释放）

- [ ] 实现主循环框架（固定时间步，驱动 ECS 调度与渲染）

---

### 3. 基础渲染抽象层

- [ ] 接入cgpu

- [ ] 接入zrenderer

---

### 4. Flecs ECS 集成与基础框架

- [ ] 初始化 Flecs 世界（`World`）

- [ ] 实现 ECS 系统调度与主循环的集成（每帧自动运行注册的 System）

---

### 5. 贪吃蛇游戏实现

- [ ] 游戏世界初始化

- [ ] 整合主循环流程：事件处理 → ECS System 调度 → 渲染

- [ ] 定义贪吃蛇核心组件

- [ ] 实现输入处理System

- [ ] 实现移动System

- [ ] 实现碰撞检测System

- [ ] 实现食物生成System

- [ ] 实现渲染System

- [ ] 实现蛇身长大逻辑

- [ ] 实现游戏重置逻辑：碰撞后重置蛇的状态、食物位置

---

### 6. 测试与验证

- [ ] 在 Windows 平台编译并运行项目

- [ ] 验证游戏正确性
