# 0.1 版本任务

目标：基于 pulse 插件体系 + ECS，实现贪吃蛇游戏（与 `examples/dascript` 同等能力）。

注：实现时参考老代码： examples/dascript

---

### 1. pulse_math — header-only 数学库

- [x] 创建 `src/pulse_math/include/pulse_math.h`，将`handmademath`复制过来
- [x] 在 `xmake.lua` 中添加 pulse_math target（header-only，无源文件）
- [x] 验证 `#include "pulse_math.h"` 可正常使用 HMM_Vec3、HMM_Quat、HMM_Mat4 等类型

---

### 2. pulse_input — 统一输入抽象层

- [x] 创建 `src/pulse_input/include/pulse_input.h`（IDL + 模板）
- [x] 定义 ECS 组件：PulseKeyboardInput、PulseMouseInput、PulseMouseMotion、PulseMouseScroll
- [x] 定义 ECS 事件：PulseKeyEvent、PulseMouseButtonEvent
- [x] 实现 PulseInputSystem：在 EcsPreUpdate 轮询 SDL 输入，更新组件并发射事件
- [x] 实现便捷查询 API（is_key_down / just_pressed / mouse_position / delta / scroll）
- [x] 在 `xmake.lua` 中添加 pulse_input target
- [x] 编写测试用例验证键盘按键状态查询
- [x] 编写测试用例验证鼠标位置和增量查询

---

### 3. pulse_transform — 变换组件与层级系统

- [x] 创建 `src/pulse_transform/include/pulse_transform.h`（IDL + 模板）
- [x] 定义 ECS 组件：Position、Rotation、LocalTransform、WorldTransform、ShowMatrix、Tree
- [x] 定义 Pipeline 标签：UpdatePipeline、PostUpdatePipeline、RenderPipeline
- [x] 实现系统 UpdateLocalTransform：由 Position/Rotation 计算 LocalTransform
- [x] 实现系统 PropagateWorldTransform：遍历 Tree 层级传播 WorldTransform
- [x] 在 `xmake.lua` 中添加 pulse_transform target
- [x] 编写测试用例验证单物体变换
- [x] 编写测试用例验证多级层级变换传播

---

### 4. pulse_renderer — 渲染管线框架

- [x] 创建 `src/pulse_renderer/include/pulse_renderer.h`（IDL + 模板）
- [x] 定义 ECS 组件：Camera（关联 window_entity）、Renderable（mesh + material）
- [x] 实现视角提取系统：遍历所有带 Camera 的 Entity，构建 View
- [x] 实现 RenderObject 排序（按 shader、material、mesh 排序以减少状态切换）
- [x] 实现双缓冲 FrameRenderPacket / ViewRenderPacket
- [x ] 实现 pulse_graphics 的 RecordCallback 提交流程
- [x] 在 `xmake.lua` 中添加 pulse_renderer target
- [x] 编写测试用例验证渲染效果

---

### 5. pulse_imgui — ImGui 集成

- [ ] 创建 `src/pulse_imgui/include/pulse_imgui.h`（IDL + 模板）
- [ ] 定义 ImGui 绘制回调类型：`PulseProcImGuiDrawFn`
- [ ] 在 pulse_window 的渲染流程末尾插入 ImGui 渲染 pass
- [ ] 提供 `pulse_imgui_add_draw_callback` API 注册绘制回调
- [ ] 在 `xmake.lua` 中添加 pulse_imgui target
- [ ] 编写测试用例验证 ImGui 叠加层显示

---

### 6. pulse_script_dascript — DaScript 脚本运行时

- [ ] 创建 `src/pulse_script_dascript/include/pulse_script_dascript.h`
- [ ] 实现 DaScript VM 初始化与销毁
- [ ] 实现模块路径管理与 NEED_MODULE 声明
- [ ] 提供 `pulse_load_script` 和 `pulse_call_script_function` API
- [ ] 在 app 主循环中插入脚本执行钩子
- [ ] 在 `xmake.lua` 中添加 pulse_script_dascript target
- [ ] 编写测试用例验证空脚本加载与执行

---

### 7. pulse_dascript_binding — DaScript 绑定层

- [ ] 创建 `src/pulse_dascript_binding/include/pulse_dascript_binding.h`
- [ ] 创建 `src/pulse_dascript_binding/src/pulse_math_das.cpp`：注册 HMM_Vec3、HMM_Quat、HMM_Mat4 等数学类型注解
- [ ] 创建 `src/pulse_dascript_binding/src/pulse_transform_das.cpp`：注册 Position、Rotation、LocalTransform、WorldTransform、Tree 等组件
- [ ] 创建 `src/pulse_dascript_binding/src/pulse_renderer_das.cpp`：注册 Camera、Light、Renderable 等渲染组件
- [ ] 创建 `src/pulse_dascript_binding/src/pulse_asset_das.cpp`：注册 AssetHandle 等资源类型
- [ ] 创建 `src/pulse_dascript_binding/src/pulse_graphics_das.cpp`：注册 ShaderHandle、MeshHandle、TextureHandle 等 GPU 句柄
- [ ] 实现统一注册入口 `pulse_register_dascript_bindings`，在 build 阶段依次调用各 binding 注册函数
- [ ] 在 `xmake.lua` 中添加 pulse_dascript_binding target
- [ ] 编写测试用例验证 binding 注册后可在 DaScript 脚本中创建和操作 ECS 组件

---

### 8. 贪吃蛇游戏实现

- [ ] 定义贪吃蛇 ECS 组件：SnakeHead、SnakeSegment、SnakeDirection、Food、GridCell
- [ ] 实现蛇移动与转向 System（通过键盘输入控制方向）
- [ ] 实现蛇生长与碰撞检测 System（自身碰撞 + 边界碰撞）
- [ ] 实现食物随机生成 System
- [ ] 实现游戏状态管理 System（得分、游戏结束重置）
- [ ] 使用 pulse_asset 加载纹理、网格、着色器等资源
- [ ] 使用 pulse_graphics 创建材质并绑定到蛇和食物
- [ ] 使用 pulse_renderer 注册渲染回调，提交蛇和食物的渲染数据
- [ ] 使用 pulse_imgui 显示游戏得分与状态信息
- [ ] 使用 pulse_script_dascript 加载蛇游戏逻辑脚本（.das 文件）
- [ ] 使用 pulse_dascript_binding 将蛇游戏 ECS 组件暴露给脚本

---

### 9. 构建与验证

- [ ] 在 Windows 平台编译所有 target 成功
- [ ] 运行 test-app、test-window、test-asset、test-graphics 全部通过
- [ ] 运行 pulse_input 测试用例通过
- [ ] 运行 pulse_transform 测试用例通过
- [ ] 运行 pulse_renderer 测试用例通过
- [ ] 运行 pulse_imgui 测试用例通过
- [ ] 运行 pulse_script_dascript 测试用例通过
- [ ] 运行 pulse_dascript_binding 测试用例通过
- [ ] 运行贪吃蛇示例，验证游戏可玩
