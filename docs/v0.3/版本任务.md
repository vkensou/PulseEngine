## 0.3 版本任务

### 1.移植datalist

已实现

### 2.基于datalist实现shader、material的加载

已实现

### 3.贴图使用ktx处理

已实现：KTX2 全格式加载（未压缩 RGBA8、原生 BC/ETC2/ASTC、UASTC、ETC1S、zstd 超压缩），Basis 运行时转码按设备能力回退（BC7→BC1/BC3→ASTC4x4→ETC2→RGBA32 无条件兜底），原生块格式不支持时显式失败并提示从母格式重转。未压缩单级文件（`--runtime-mipmap`）在加载时由引擎生成 mip 链，压缩/cubemap 只 warning 按层上传。见 `docs/v0.3/ktx2贴图加载设计.md`（§11 为复核修正记录）与 `tools/ktx/README.md` §9。遗留：纹理数组/3D 资产、cubemap 端到端未测、RGB8 缺 SAMPLE 是否展开成 RGBA8 待定、转码移工作线程、裁剪 BASISD_SUPPORT_ATC/FXT1/PVRTC1/PVRTC2 减包体（本期按约定暂不裁）。

### 4.数据表格

### 5.场景序列化